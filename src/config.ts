/**
 *  Executable Language Modifier Which will change the last $dir to ./ in Powershell
 *  as the current Directory is not in PowerShell's Path
 */
export default {
	executableLanguages : ["c", "cpp", "go", "rust"],
};